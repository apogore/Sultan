import React from "react";
import "./footer.css";

const Footer = ({ className }) => {
    return (
        <footer className={className}>
            <div>Футер</div>
        </footer>
    );
};

export default Footer;