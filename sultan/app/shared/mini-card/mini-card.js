import React from "react";
import "./mini-card.css";

const MiniCard = ({ className }) => {
    return (
        <div className={className}>
            <div>Мини-карточка</div>
        </div>
    );
};

export default MiniCard;